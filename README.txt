Requires Python 3.X

No external libraries are used other than the defaults:
- datetime, os, sys, json, math

Example usage:
`python3 rolling_median.py path_to_input.txt path_to_output.txt`

There is a bottleneck in finding the median.
